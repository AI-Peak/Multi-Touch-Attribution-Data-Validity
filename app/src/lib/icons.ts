/**
 * Curated icon re-exports. Pulling from lucide-react one place keeps
 * the bundle predictable and the icon vocabulary consistent.
 */
export {
  AlertTriangle as IconWarn,
  Info as IconInfo,
  Check as IconCheck,
  X as IconX,
  Send as IconSend,
  ArrowRight as IconArrowR,
  ArrowDown as IconArrowDown,
  FileText as IconDoc,
  Sparkles as IconSparkle,
  ChevronDown as IconChevronDown,
} from "lucide-react";
